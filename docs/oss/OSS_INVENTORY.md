# OSS inventory

The machine-readable direct inventory is `oss-inventory.json`; the complete transitive graph is generated as CycloneDX JSON at `artifacts/sbom.cdx.json` by `pnpm run sbom:generate`. `pnpm licenses:check` evaluates every installed package.

The M2 scanner also has a container-specific CycloneDX 1.6 inventory at `artifacts/container-sbom.cdx.json`, generated by `pnpm sbom:container`. Its immutable inputs are ClamAV 1.4.6 at digest `sha256:e6444f72de025a3d57e2820dd1ad9f8734d1d4d6ab0e3336cdeb09fa2ba2f122` (GPL-2.0-only scanner project) and Node.js 24.19.0 Bookworm slim at digest `sha256:65932751ed4073ed02f5c04e494e4b2572a891b7dbea0568a863dc80341bf848` (MIT Node.js project, with Debian package licenses retained in image metadata). The worker installs the MIT `@napi-rs/image-linux-x64-gnu` 1.13.0 binary for its actual Linux amd64 target. Other exact 1.13.0 platform binaries remain optional lockfile entries and are not all shipped in that image. The same worker installs FFmpeg 5.1.9-0+deb12u1 from the immutable Debian 2026-08-23 snapshot and exposes only fixed internal `ffmpeg`/`ffprobe` paths for bounded remux.

ClamAV hosted activation and image distribution are **NOT RUN**. The pinned image is repository/local operational evidence only; separate legal and operator approval must confirm applicable GPL source-offer and notice compliance before any distribution or hosted activation. It is not represented as permissive or production-approved.

The exact Debian FFmpeg 5.1.9 build is GPL-2.0-or-later and its dependent codec packages retain their individual licenses. Hosted activation and worker-image distribution are **NOT RUN** and require separate legal/operator approval plus applicable GPL source/notice compliance. Static JPEG/PNG/WebP still use full native decode/re-encode; approved M4A/MP4 audio and completion MP4 video use actual metadata-stripping stream-copy remux and reopen; PDF, WebM, unsupported codecs, and malformed containers fail closed.

Primary runtime: Expo 57.0.15, React Native 0.86.2/React 19.2.3, Next 16.3.1/React 19.2.8, Supabase JS 2.112.3, TanStack Query 5.101.4, React Hook Form 7.85.0, Zod 4.4.3, `@napi-rs/image` 1.13.0, i18next 26.3.6, OpenAI Node 7.5.0, noble-hashes 2.3.0. Tooling: pnpm 11.19.0, Turbo 2.10.10, TypeScript 6.0.3, Supabase CLI 2.114.0, Vitest 4.1.10, Playwright 1.62.1.

No third-party source directory, application, brand asset, or binary is vendored. Maestro and k6 are external operator tools. See `OSS_EVALUATION.md` and `THIRD_PARTY_NOTICES.md` for decisions and exceptions.
