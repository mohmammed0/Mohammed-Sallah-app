# دليل المساهمة العربي

[بوابة المساهمة](CONTRIBUTING.md) · [English](CONTRIBUTING.en.md)

## قبل البدء

1. اقرأ [AGENTS.md](AGENTS.md) و[حالة النسخة التجريبية](docs/status/CLOSED_BETA.md).
2. تأكد أن العمل ضمن Issue أو milestone معتمد، وأن فرعك يبدأ من رأس beta الموافق عليه.
3. استخدم Node 24 LTS وpnpm 11.19.0 وDocker وSupabase CLI 2.114.0.
4. لا تنشئ اعتماداً أو سراً أو خدمة مدفوعة لتجاوز بوابة بشرية.

## نموذج الفروع

- `main` للاستقرار أو الإصدارات المنشورة فقط.
- فرع RC هو فرع التكامل الأب لمرشح الإصدار.
- فرع beta هو تكامل النسخة التجريبية المغلقة.
- فرع milestone ابن محدود من رأس beta المعتمد.

لا تدمج أو تدفع أو تنشر إلا إذا طلب مالك مخول ذلك صراحة.

## قواعد الكود والبيانات

- حافظ على modular monolith؛ قواعد النطاق في `packages/domain` أو أوامر قاعدة البيانات الذرية.
- استخدم TypeScript الصارم وZod لكل حد ثقة واتحادات exhaustive بلا `any` ضمني.
- الأموال أعداد صحيحة بوحدات SAR الصغرى؛ الوقت UTC ويعرض بتوقيت Asia/Riyadh.
- لا تعدل migration مطبقة. أنشئ migration زمنية forward-only.
- فعّل RLS لكل جدول مكشوف وابدأ بالمنع افتراضياً.
- كل RPC حساس يحتاج مصادقة وتفويضاً وإعادة محاولة idempotent وفحص نسخة ومعاملة وتدقيقاً.
- أضف pgTAP لحالات السماح والمنع المتقاطعة، خصوصاً المواقع الدقيقة والعروض المنافسة.
- أعد توليد `packages/database/src/database.types.ts` وأثبت عدم وجود drift.

## اللغة وإتاحة الوصول

- العربية هي مصدر تصميم النص.
- كل نص ظاهر يستخدم مفاتيح مشتركة للعربية والإنجليزية والأردية والهندية.
- اختبر RTL/LTR ولوحة المفاتيح والتركيز والنص الكبير والتباين وتسميات قارئ الشاشة.
- أضف حالات loading وempty وerror وoffline وretry وsuccess وdestructive حسب السطح.

## التبعيات

لا تقبل إلا تبعيات مراجعة بترخيص permissive مع نسخة exact. عند الإضافة حدّث
`oss-inventory.json` و`THIRD_PARTY_NOTICES.md` وlockfile، وشغّل فحوص الترخيص والأمن. لا
تورد تطبيقات كاملة أو branding منقولاً أو binaries غير مراجعة، ولا تُخفِ نتائج التدقيق أو
تتجاوزها.

## التحقق

```bash
pnpm install --frozen-lockfile
pnpm docs:check
pnpm format:check
pnpm lint
pnpm typecheck
pnpm test
pnpm validate
```

لتغيير قاعدة البيانات:

```bash
supabase db reset
pnpm test:db
```

لا تسجل PASS لما لم يُشغل. غياب Docker أو المتصفح أو الجهاز أو الاعتماد يعني NOT RUN.

## Commit وPull Request

- استخدم Conventional Commits لمراحل متماسكة ومتحققة.
- يجب أن يذكر PR النطاق والتطبيقات وmigrations والأمن والخصوصية والـ OSS والنتائج الفعلية
  والمدخلات البشرية والمخاطر المتبقية واللقطات وخطة التراجع.
- لا تكتب أسراراً أو حسابات حقيقية أو PII أو عنواناً دقيقاً أو مستنداً أو رسالة أو بيانات دفع.
- تغييرات قاعدة البيانات تتراجع بمسار تعويضي أو استعادة معتمدة، لا بإعادة كتابة التاريخ.
